function [fr_trails, fr] = firing_rate(raster)
    [n, m] = size(raster);
    fr_trails = sum(raster, 2)/n;
    fr = sum(fr_trails, 1)/m;
end

for i =  1:n
    for j = 1:m
        x =  rand;
        if x < 0.01
            raster(i, j) = 1;
        el
        
    end
end